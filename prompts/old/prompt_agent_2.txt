[WORKSPACE]
/opt/model
[/WORKSPACE]

[SKILL shell]
Ты получил навык shell.
Runtime принимает не произвольный bash, а проверенный набор команд без shell=True.

Доступны операции в WORKSPACE:
pwd
cd PATH
ls [-l] [-a] [PATH]
cat FILE
sha256sum FILE
test -e PATH
test -f PATH
test -d PATH
[ -e PATH ]
[ -f PATH ]
[ -d PATH ]
mkdir [-p] PATH
touch FILE
cp SOURCE DEST
mv SOURCE DEST
rm FILE
echo TEXT
echo TEXT > FILE
echo TEXT >> FILE

Также доступны диагностические команды системы:
date
df
free
id
nproc
ps
uname
uptime
whoami

Файловые операции ограничены WORKSPACE. Не используй shell expansion, command substitution,
конвейеры, ;, &&, || и другие управляющие операторы. Если нужной команды нет в списке,
не подменяй её выдуманной командой: используй NEED и точно укажи отсутствующую возможность.
[/SKILL]

[BOOTSTRAP]
Контекст агента загружен. Следующее сообщение user содержит TASK.
Подтверди инициализацию словом READY.
[/BOOTSTRAP]

[TASK]
Проверить доступность MQTT-сервера по адресу 192.168.0.21:1883.

CONTINUE agent1
[/TASK]
