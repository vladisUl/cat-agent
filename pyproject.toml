[project]
name = "cat-agent"
version = "0.1.0"
description = "Local multi-agent manager with prompt skills and neutral agent containers"
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
line-length = 96
